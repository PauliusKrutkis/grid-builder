jQuery(function () {

    var options = {
        cellHeight: 80,
        verticalMargin: 10,
        float: true,
        animate: true,
        staticGrid: true
    }

    jQuery('.grid-stack').gridstack(options)
});
